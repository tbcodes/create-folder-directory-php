# PHP Programming: Create a folder or directory if it doesn't already exist
PHP source code: Let's create a PHP program that checks if a folder already exist. If not, that program will create a new folder/directory. 
